# AMD ROCm images

Reserved for future `linux/amd64` ROCm inference images.

Petabridge currently consumes stock ROCm images. No AMD derivative is defined
or built by this repository today. Future images will use the same manifest,
candidate, SBOM/provenance, and digest-promotion gates as the DGX Spark family.
